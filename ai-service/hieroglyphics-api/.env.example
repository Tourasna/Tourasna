# Copy this file to .env and adjust values as needed
# .env is gitignored - never commit it

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=False

# Device (auto/cuda/cpu)
DEVICE=auto

# Detector thresholds (tune if needed)
DETECTOR_CONF_THRESHOLD=0.25
AMBIGUITY_THRESHOLD=0.60